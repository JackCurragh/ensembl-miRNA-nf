Bootstrap: docker

From: ubuntu:20.04

%post
    apt-get update
    apt-get --update install libxml2-dev libxslt-dev libffi-dev gcc musl-dev libgcc openssl-dev curl
    apt-get install jpeg-dev zlib-dev freetype-dev lcms2-dev openjpeg-dev tiff-dev tk-dev tcl-dev
    apt-get install make automake gcc g++ subversion python3-dev zlib-dev libjpeg
    pip install ete3 numpy biopython matplotlib pandas
